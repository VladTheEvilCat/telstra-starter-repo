spring:
  datasource:
    url: jdbc:h2:~/sim_card
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

services:
  sim-card-actuator-service:
    url: http://localhost:8444